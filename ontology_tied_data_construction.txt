Your task is to construct a dataset of 100 atomic questions tied to the extracted TTL ontology, following these exact rules:

Ontology presence split:

50 questions where the answer IS present in the extracted TTL ontology.

50 questions where the answer is NOT present in the extracted TTL ontology.
Set the field "ontology_presence" to "present" or "absent" accordingly.

Question type distribution:
For each 50-question bucket (present and absent), create exactly:
definition: 6
taxonomic: 6
capability: 6
property: 6
membership: 6
comparative: 8
quantification: 6
existential: 6
Total per bucket = 50.

Entity-type balancing:
For each question type, create 3 questions using a class entity and 3 questions using an instance entity.
For comparative questions (8 total), create 4 using a class entity and 4 using an instance entity.

Answer-form distribution:
Across each 50-question bucket, use exactly:
20 entity answers
10 literal answers
10 boolean answers
10 list answers

Ontology-present questions:
For ontology_presence = "present":
The answer must be derivable solely from the entity context.
For comparative questions, the answer must be derivable from entity context plus object context.
Use only entities, relations, and literals that exist in the extracted TTL ontology.
No invented facts.

Ontology-absent questions:
For ontology_presence = "absent":
The answer must NOT be derivable from the entity context (or object context for comparative questions).
Create absence by using entities not in the ontology, relations not in the ontology, combinations not present, or contradictory or invented facts.
For absent questions:
expected_decision = "abstain"
gold_answer = ""
gold_mapped_answer must contain empty lists and empty dicts for both entity_side and object_side.
acceptable_labels must include: "i don't know", "unknown", "not in ontology", "not found", "can't answer the question".

JSON format for each question:
Each question must be a JSON object with the following exact structure:

{
"id": "...",
"atomic_input": "...",

"question_type": "...",
"answer_form": "...",

"entity": {
"value": "...",
"type": "class or instance"
},

"relation": "...",

"object": {
"value": "...",
"type": "class or instance or literal or null"
},

"ontology_presence": "present or absent",

"gold_answer": "...",

"gold_mapped_answer": {
"entity_side": {
"types": [...],
"superclasses": [...],
"equivalent_classes": [...],
"properties": [...],
"property_values": { ... }
},
"object_side": {
"types": [...],
"superclasses": [...],
"equivalent_classes": [...],
"properties": [...],
"property_values": { ... }
}
},

"expected_decision": "answer or abstain",

"acceptable_labels": [
"..."
]
}

For non-comparative questions, set "object_side" to null inside gold_mapped_answer.

Output format:
Create a json-file with the 100 question objects as requested.