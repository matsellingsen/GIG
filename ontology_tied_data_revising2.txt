Your task is to validate and correct the entity, relation, and object fields for each of the 100 QA examples.

I am giving you as context:
The JSON array of 100 questions.

Your job is to ensure that the labels follow the system’s extraction rules. 

RULES FOR ENTITY (SUBJECT)
The entity must appear verbatim in the atomic_input.

No rewriting, paraphrasing, or normalization.

If the entity is multi‑word, extract the full span.

The entity must be the subject/container of the question, not the object.

RULES FOR RELATION
The relation must be chosen only from the allowed set for the question_type:

The relation must match the linguistic structure of the atomic_input.

The relation must not be invented or paraphrased.

RULES FOR OBJECT
If the question_type requires an object, it must be:

explicitly present in the atomic_input, OR

a literal value extracted from the question, OR

unknown if the system is designed to leave it unresolved (e.g., property‑value questions).

The object must not be hallucinated or rewritten.

For comparative questions, the object must be the comparison target mentioned in the atomic_input.

CORRECTION BEHAVIOR
If any of the three fields (entity, relation, object) violate the rules:

Correct only the incorrect fields.

Do not rewrite the atomic_input.

Do not modify question_type or answer_form.

Do not add ontology facts not present in the TTL.

Keep changes minimal and faithful to the extraction rules.